export interface GameSettings {
     id: string;
     keyboard_font: string;
     keyboard_theme: string;
     wpm_visible: boolean;
 }

export interface UserSettings {
     id: string;
     theme_mode: string;
}