import GameTextContentLang from "./Enums/GameTextContentLang"

type GameTextContentParams = {
    lineAmount: number,
    lineLength: number,
    lang: GameTextContentLang,
}

export default GameTextContentParams;